
import 'package:flutter/material.dart';

class ProfileCard {
  final String title;
  final String subTitle;
  final String description;
  final Image icon;

  ProfileCard(this.title, this.subTitle, this.description, this.icon);
}